extension Angle : Hashable, Comparable {
    var hashValue: Int {
        //  implementing hashValue property
    }
    
    static func < (lhs: Angle, rhs: Angle) -> Bool {
        // implementing less than operator
    }
}
